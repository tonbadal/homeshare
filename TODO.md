we need to improve the invite flow. i don't like inviting via link only, as it can create some friction. it should alow the user to invite an email, so that if the email already has an account, it should just add the house.

also, in the invite pop up, let's add a selector so that we can select which house you are inviting the new user. 

and this should create users in "invited" state, so that they are already present in the platform and can have some bookings 